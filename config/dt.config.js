var DigiTrustConfig = DigiTrustConfig || (function () {
 var I = {};

 var dtIframeOrigin = I.dtIframeOrigin = "${iframe.origin}";

 var dtIframeSource = I.dtIframeSource = "${iframe.source}";

 var dtUsagePixelSource = I.dtUsagePixelSource = "${usage.pixel.source}";

 var dtIdentityCreateURl = I.dtIdentityCreateURl = "${identity.create.source}";

 return I;
}());
